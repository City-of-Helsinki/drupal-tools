on:
  pull_request:
  push:
    branches:
      - main
name: CI
jobs:
  tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        php-versions: ['8.1', '8.2']
    container:
      image: ghcr.io/city-of-helsinki/drupal-php-docker:${{ matrix.php-versions }}-alpine

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 1

      - name: Install required composer dependencies
        run: composer install

      - name: Run PHPCS
        run: vendor/bin/phpcs --standard=Drupal --ignore=*/vendor/* .

      - name: Run PHPUnit tests
        run:
          vendor/bin/phpunit -c tests/phpunit.xml --bootstrap tests/bootstrap.php \
          --coverage-clover=coverage.xml \
          tests/

      - name: Upload codecov
        run: codecov

